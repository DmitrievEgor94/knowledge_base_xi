xi_marks @

Headings .
.  For headings `space-dot` are used at the `end`, for example 'Heading 1 .'. If you want to make the second 
level paragraph use `two spaces` at the `beginning`, '  Heading .'. For another level two spaces are added again and so on.

Paragrahs .
